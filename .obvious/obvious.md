# build-something-cool — Obvious agent guide

## Repository

`build-something-cool` — a playground for Obvious.ai projects.

**This repository is currently empty.** As of 2026-09-03 the only tracked file is `README.md` (a title and the line "Playground for Obvious.ai projects"). There is no application code, no dependency manifest, no Dockerfile or Compose file, and no Makefile.

## Stack

Not determined. No dependency manifest or build configuration exists to inspect, so no runtime, framework, package manager, or service is claimed here.

## Commands

None. There is nothing yet to install, run, test, lint, or build in this repository.

## Codebase map

Omitted. There is no code to map; this section will be filled in by the next setup run.

## Local verification

Not applicable. This onboarding run took the EMPTY REPOSITORY path: with no application present there is no dev stack to bring up, so `dev_stack_healthy` is `false`, no primary user flow was exercised, and no sandbox snapshot was captured.

## Next setup run

When an application is added to this repository, the next Obvious setup run will:

- bring up a working local dev environment and verify a primary user flow end-to-end,
- capture a live sandbox snapshot,
- populate the stack, commands, codebase-map, and local-verification sections of this file,
- add `.obvious/codebase-map.md` and `.obvious/skills/local-dev/SKILL.md`.

---

*Generated by the Obvious onboarding worker (empty-repository path) on 2026-09-03.*
